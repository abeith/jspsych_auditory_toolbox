import { simulateTimeline, startTimeline } from "@jspsych/test-utils";

import avAudioResponse from ".";

jest.useFakeTimers();

describe("av-audio-response simulation", () => {
  test.skip("nothing", () => true);
});
